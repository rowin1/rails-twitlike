<% provide(:title, "Users") %>
<h1>All users</h1>

<%= will_paginate %>
<ul class="users">
  <!--
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
  -->
  <%= render @users %> <!-- Old code shown above -->
</ul>
<%= will_paginate %>
