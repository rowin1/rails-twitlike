<% provide(:title, 'Edit user') %>
<% provide(:button_text, 'Save changes') %>
<h1>Update your profile</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <a href="http://gravatar.com/emails" target="_blank" rel="noopener">change</a>
    </div>
  </div>
</div>

<!--
How does Rails know how whether to use a POST request for a new user or a PATCH
request for editing users? When constructing a form using the 'form_for(@user)'
method, Rails uses the 'new_record?' method and uses POST if true and PATCH is false.
-->
